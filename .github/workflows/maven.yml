name: Java CI with Maven

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'maven'

    - name: Install xvfb (skipped in debug)
      run: |
        echo 'Skipping apt-get install step for debug; previous runs failed here.'
        echo 'If tests need Xvfb, re-enable this step.'

    - name: Build and Test with Maven (Xvfb)
      working-directory: ./auctionweb
      run: |
        set -eux
        echo 'Start Xvfb on :99'
        Xvfb :99 -screen 0 1280x1024x24 &
        export DISPLAY=:99
        echo 'Environment dump:'
        env | sort
        echo 'Maven version:'
        mvn -v
        echo 'Run mvn with debug flags (temporarily skipping tests to isolate apt/Xvfb issues)'
        mvn -B -X -e -Djava.awt.headless=true -DskipTests clean package | tee mvn-test.log
        echo 'mvn exit code:' $?
        tail -n +1 mvn-test.log | sed -n '1,200p'
